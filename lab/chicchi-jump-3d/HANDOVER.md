# チッチジャンプ3D — 引き継ぎ

（新しいセッションの最初に、これをそのまま貼ってください）

---

## いまやっていること

`c:\Users\User\Desktop\Claude\tuna app\lab\chicchi-jump-3d\` で、
既存の2D版「チッチジャンプ2」(`js/jump.js`) を**残したまま**、
外部ライブラリ0の自作WebGL2でフル3D化した新作を作っています。

**まず読むもの（順に）**
1. `C:\Users\User\.claude\plans\2-3d-aaa-hidden-quasar.md` … 全13章の計画書
2. 記憶 `project_chicchi_jump_3d.md` … 決定ずみの方針と、踏んだ地雷
3. `lab/chicchi-jump-3d/js/` の各ファイル冒頭コメント

## 動かしかた・確かめかた

```
cd "C:\Users\User\Desktop\Claude\tuna app"
python -m http.server 8899
```
- 単体で見る: `http://localhost:8899/lab/chicchi-jump-3d/index.html`
- 本体から  : `http://localhost:8899/index.html` → 息抜きタイム → チッチジャンプ3D

**実測（見つもりで判断しない。必ず撮って見る）**
```
python lab/chicchi-jump-3d/tools/shot.py   # チッチのモデル12カット
python lab/chicchi-jump-3d/tools/play.py   # 自動操縦で実プレイ＋各ゾーン
python lab/chicchi-jump-3d/tools/sky.py    # 深宇宙の背景を1種類ずつ正面に持ってきて17枚
python lab/chicchi-jump-3d/tools/climb.py  # 公園→ビル→成層圏→カーマンライン→地球→惑星（13枚）
python lab/chicchi-jump-3d/tools/helmet.py # ヘルメットの寄り2枚
python lab/chicchi-jump-3d/tools/rock.py       # 🪨 宇宙の足場を撮る＋画面上の幅と高さを数える
python lab/chicchi-jump-3d/tools/rock_ab.py    # 🪨 厚み・奥ゆきの見くらべ（スマホ形とPC形の両方）
python lab/chicchi-jump-3d/tools/rock_stand.py # 🐤 チッチが岩に乗ったところ（かぶりの確認）
python lab/chicchi-jump-3d/tools/flicker.py    # 🖥 チカチカを数でさがす（1コマおきの明滅を検出）
node   lab/chicchi-jump-3d/tools/biome_check.js    # 道のりの機械検査（連続重複・なめらかさ）
node   lab/chicchi-jump-3d/tools/physics_check.js  # 物理がfpsに左右されないこと（プラン最大の落とし穴）
```
`?seed=20260820` を付けると、いつ開いても同じ道のりになる（直す前と後で見くらべるため）。

## ファイルの役割

| ファイル | 中身 |
|---|---|
| `js/gl.js` | WebGL2土台。行列・シェーダ・**ロフト/管/球/環**のジオメトリ生成・`fixWinding` |
| `js/shaders.js` | GLSL。トゥーン＋スキニング／背面法アウトライン／インスタンス／つぶ／空 |
| `js/chicchi.js` | チッチ本体（断面ロフトの一体成型メッシュ・骨9本・アニメ状態機械・顔テクスチャ） |
| `js/props.js` | 足場（雲/バネ/氷/こわれ/小惑星）・アイテム・じゃま役・ボイジャー |
| `js/scenery.js` | 背景（ビル・スカイツリー・富士山・太陽・地面・地球・月・惑星・飛行機・人工衛星・ヘルメット） |
| `js/core.js` | ゲームのルール（描画を知らない層）。物理・足場抽選・距離・天気 |
| `js/renderer.js` | 描画。カメラ・インスタンス・つぶ・接地影 |
| `js/biome.js` | **深宇宙の背景を「数字の集合」で表す。7種袋の巡回・名前つきの場所・色あいの乱数** |
| `js/park.js` | **はじまりの公園**（芝生・木・ベンチ・ラジオ・バネの遊具・街灯） |
| `js/game.js` | 入力・ループ・演出・風景の配置・本体とのpostMessage |

## 🚨 何度も踏んだ地雷（作業前に必ず読む）

### 1. メッシュが裏返る → 真っ黒な塊になる（**6回**踏んだ）
本体パスで裏面カリングされて消え、輪郭パス（表を切って裏を描く）だけが残るため。
- 目のドームの基底が**左手系**だった（列が X×Y≠Z）
- 足のロフトを**下向き(-Y)に作った**
- ロフトの**ふたの巻き順**が逆
- `tubeGeometry` の巻き順がロフトと逆
- `ringGeometry` の巻き順が逆（地面が真っ黒な円ばんに）
- 皿の内外を**形ごと反転**させた（背中あわせのおわん2つになった）

→ **新しいジオメトリを書いたら必ず `fixWinding(pos, nrm, idx)` を通す。**
→ ロフトは必ず +Y 方向に作り、下向きにしたいときは骨/配置の回転で倒す。
→ 内がわ／外がわは「同じ形をうすくずらした2枚」。形を反転させない。

### 2. 背景の置き場所
- **カメラは筒の軸から約9.65（`CAM_AXIS`）離れている。** 距離の基準は必ず「カメラから」。
- 背景は**筒の向こうがわ**（角度に π を足す）に置く。手前に置くと近すぎて巨大化する。
- 置き場所は `bgPlace(depth, lat, elev)` を使う。`depth` は重なり順のためだけで、
  **大きさは実寸と実きょりから `sizeAt()` で出す**（`groundView()` が見上げ角を返す）。

### 3. 縮尺は1つに載らない
200mのビルと半径6371kmの地球は同じ物差しに載らない。3枚を高度で受けわたす。
- 〜約1,400m … **公園の芝生**（`buildParkGroundMesh`。世界に固定＝登ると遠ざかる）
- 400m〜約20万m … **平らな地図**（`buildFlatGroundMesh`。海岸線・山なみ・川）
- 12万m〜 … **地球の球**（`buildEarthMesh`。日本が上を向く）＋ **大気のふち**
- **低いところで球を使うとカメラが球の面に乗り、地面が薄い帯になって下が黒く抜ける。**

### 4. 空のもの（星・星雲・銀河）を置くとき
- **カメラは 0.17rad 見おろしていて、縦の画角は ±0.58rad しかない。**
  見上げ角 0.4 より上に置くと画面の外に出て、二度と見られない（銀河が左上に切れていた）。
  横の画角は **0.34rad** しかないので、2つ並べるものは 0.3rad 以内に置く。
- **画面のどこに映るかは `__cj.skyScreen(向き)` で数えられる。** ただし
  **カメラの行列は次の rAF で作られる**ので、向けた直後ではなく1フレーム描いてから測る。
- **にじみ（グロー）は端を必ず0まで落とす。** 途中で `if (d > …) return` すると
  空に丸い切り口が見える（赤い星のまわりに円のふちが出た）。
- **背景が明るいと暗い岩の足場が沈む。** 広い面（帯・星雲・銀河・にじみ）だけ
  まとめて頭打ちにしてある（`CEIL = 0.20`）。星とビーコンの芯は点なので対象外。
  （0.34 では緩すぎて、実測で空0.358・岩0.368＝コントラスト0.03 だった）

### 5. 実測の落とし穴（今回踏んだ）
- **撮った絵のフォルダを消さずに撮り直すと、前の回の残りを見てしまう。**
  種（seed）が毎回ちがうと、同じ名まえのファイルが別の回のものになる。
  → `tools/sky.py` は毎回フォルダをまっさらにし、**種を固定**するようにした。
  これに気づかず「直したのに絵が変わらない」と30分ほど回り道した。

### 6. 地面と地平線（ここで2回はまった）
- **地面のふちは「本当の地平線」に合わせる。** `Rg = Dg / tan(dip)`。
  勝手な見込み角で決めると、地面のふちだけ下にずれて、
  同じ地面の上にあるはずの**富士山とビルが空に浮く**（本人に指摘された）。
- 逆に、**目の高さのすぐ下に大きな板を置くと画面の下半分が地面になる**。
  はじめ 0.36 下に半径330 を置いていて、**チッチが地面に埋もれて見えた**。
- 平らな地面のもようは **半径の対数**で作る。ふつうに半径で作ると、
  低いところでは外がわの1割しか視野に入らず、海岸線も山も一度も見えない。
- **円ばん（ringGeometry）は頂点が内と外の2周しかない。** 同心に何枚も重ねないと、
  colFn で描いたもようが全部「2色の補間」に潰れる（公園が土一色になった）。
- **大気のからは、カメラの内がわに入れない。** `rs = r + min(r*0.055, (D_E-r)*0.5)`。
  内がわに入ると裏面カリングで丸ごと消える。

### 7. その他
- 顔テクスチャは `UNPACK_FLIP_Y_WEBGL` が要る（無いと顔のオレンジがおなかに出る）
- 足場の**見た目の上のはしは実測値**（`PropBuilder.build()` の `top`）を使う。定数だとチッチが沈む/浮く
- 黒い宇宙に半とうめいを重ねると暗くなる。ガラス（ヘルメット）は**加算合成**
- 単位は必ず小さい順（`AU → 光分` は逆。1光分 < 1AU）
- 高さでメッシュごと引き伸ばす物（ビル）に別パーツの板を貼らない。頂点カラーの帯にする
- テスト用 `warp(m)` は**カメラと足場も一緒に運ぶ**こと（数字だけ進めると別の枠で確認してしまう）
- ポーズの撮影時は rAF ループを止める

## すんでいること

- チッチの3Dモデル（ロフト一体成型・骨9本・トゥーン＋輪郭・視線制御・呼吸・まばたき）
- 円筒タワー＋物理の1:1移植（**dt秒＋固定サブステップ**。元は60fps固定なので必須）
- 二重ルート（安全な道／タワーの反対がわの危険な道）
- 足場を種類ごとにモデリング（バネ＝コイル／氷＝結晶／こわれ＝割れ／宇宙＝小惑星）
- じゃま役4種（タカ→気球→人工衛星→宇宙人）
- 音（WebAudio自前合成。**高度でローパスを閉じる＝空気が薄くなる**／BPMも下がる）
- つぶ4種（けむり・火花・破片・きらめき。合成を種類で分ける）
- ヒットストップ／カメラのゆれ／接地影
- ゲーム性：ジャストジャンプ／たたむ／落ちた速さがバネの威力／今日の宇宙天気10種
- 距離システム（`rawM`＝難しさ／`progress`＝表示。実距離は対数補間。**全域で単調増加を機械検証ずみ**）
- ボイジャー1号（progress 15,000。約34秒並走してから追いこす。セリフ7行）
- 背景（ビル群→スカイツリー→富士山に沈む夕日→地面→地球→月→惑星→飛行機）
- 宇宙ステーション(2000)でヘルメット装着（**ガラスに描き直した**。fresnel でふちだけ光らせ、
  正面はほぼ透ける＋光の当たる一点だけハイライト。前は黒いかぶりものに見えていた）
- **🌌 深宇宙の背景（biome）**（2026-08-20）
  - 空を**「見ている向き」で描く本物のスカイボックス**に作りなおした
    （前は画面のuvで散らしていたので、カメラが回ると星が画面に貼りついていた）
  - 描けるもの：星3層／天の川の帯（広いぼんやり＋明るい芯）／散光星雲／暗黒星雲（星をさえぎる）／
    渦巻銀河（腕・バルジ・暗い筋）／球状星団（つぶつぶ）／明るい星（十字の光条・明滅1.2Hz）／
    **ブラックホール（見る向きそのものを曲げる重力レンズ＋降着円盤のリング）**
  - **決まった道のり**：〜13,000 なし → ヘリオポーズ → 星の海 → オールトの雲 →
    **30,000 から7種袋の巡回**（10種・1回1,200〜2,200・切りかわりは250かけて数字を混ぜる・
    出るたび色あいを±0.045振る／名前に色が入っているものは振らない）。
    **名前つきの場所**（オリオン大星雲55,000／天の川の中心70,000／
    天の川を出る85,000／アンドロメダ100,000）が巡回に割りこむ
  - 中景の**塵**が流れる（遠景は動かないのに塵は流れる＝広さの体感）
  - 場所が変わると名まえを1行だけ出す（📍到達地点と同じ名まえのときは出さない）
  - 機械検査ずみ：すきま/重なり0・連続重複0・名前つきの場所が正しい位置・
    値がすべて有限・向きの長さ1・明滅≤2Hz・1歩の変化 最大0.006（なめらか）
  - 実測：17種すべて画面内・コンソールエラー0・空のぶんの重さ +4ms（swiftshader上）
- **🌳 はじまりの公園 ＋ 打ち上げ演出**（2026-08-20）
  ベンチのラジオから **ボイジャー1号の声** → 「その声は、23時間まえの声。」→
  「へんじを、しに行こう。」→ **バネの遊具でズギューン**。
  **宇宙を目指す理由＝電波では間に合わないから、自分で返事をしに行く。**
  2回目からは短い版（1.3秒）。**画面をさわればいつでも飛べる**。
  打ち上げ直後は雲を3つ縦に重ねてあるので、外して即死しない
- **🌍 序盤の見え方を、本物の高度に合わせて作り直した**（2026-08-20）
  空の色は**進行度ではなく本当の高度**で決める（前は「5km」でもう夜空だった）。
  公園 → ビルのあいだ → 成層圏の濃い青 → **地平線の青い帯**（カーマンライン）→
  地球の球＋**大気のふち** → 青い玉。地面は海岸線・山なみ・川のある地図
- **🧭 次の足場の向き**（2026-08-20）
  バネで一気に上がって足場が画面の外へ行ったときだけ、はしに向きが出る。
  見えているときは何も出さない
- **本体組込みは、いったん外れている**（下の「注意」を見ること）

- **🎬 プランの残り（2026-08-21 にまとめて実装）**
  - §2.3 カメラの非対称追従（上りk=8／下りk=3）／ミスなしで視界が広がる／**✨演出トグル**
  - §3.1〜3.3 ⛸氷のスケート／💥こわれ雲の反動+15%／⚡逆フリック／👀指を止めると先が見える／空中コンボは着地でリセット
  - §3.4 **🌈レリックと光の柱**（2本立って、寄ったほうの力が手に入る。最大3・そのラン限り）
  - §3.5 **難易度の自動調整**（着地でじわっと上げ、ひやっとしたら即やさしく＝寛容がわに非対称）
  - §3.6 落ちたあとの引き／換算3行（歩いたら・新幹線なら・光なら）／**手ぶらで終わらせない**
  - §3.7 **天体スタンプ帳**／**きのうの自分の標**／**左端の帯**（常設UIはこれ1本だけ）
  - §5.1 **光の逆二乗**（土星から先はチッチが自分で光る）／**木星の下に地球11個**（実寸・文字なし）／**光と並走**
  - §5.2 **完全な無音3回**／§5.3 **ヘリオポーズの分水嶺**（予兆→膜→無音→星図→太陽に輪→ふり返り）
  - §5.6 **🔵地球の青い点は最後まで消さない＋さわると光る**／レコードの「名前のない1曲」／
    **🐦2匹目のチッチ**（ボイジャーまで行った人だけ・説明なし）／プロキシマの1行
  - §7 **今週のシード**（週がわりの同じ地形＝兄弟が同じ条件）／**自己ベスト更新幅**／**協力メーター**
  - §5.6 **✉️ 父から息子への手紙**（アンドロメダ＝250万光年で**一度きり**・14枚・
    さわると1枚ずつ・**署名なし**）。★**本人の言葉**。画面の幅に合わせて区切っただけ。
    **言いまわしを勝手に整えないこと。** 直すときは必ず本人に確認する
  - タイトル画面（本人のキーアート）・**キーボード操作**・**道具を使うと点が入らない**
  - 🚨 **PCでチカチカする不具合**を修正（画質の自動調整が毎フレーム canvas を作り直していた）

## まだのこと

**プランのうち、やらないと決めたもの（理由つき）**

- **§2.1 タワーの直径を高度で変える** … プラン自身の**より強い決めごと2つ**とぶつかる。
  ①「ドラッグ感度は現行と一致するようにキャリブレーションする」②「カメラのせいで死なせない」。
  半径を変えると px（弧の長さ）と画面の対応がずれ、**見えていた足場に乗れない**が起きる。
  難しさの調整は §3.5 の自動調整でもう効いているので、無理に入れない
- **§3.7 下ドラッグで1秒だけ下を向く** … **プランの中で下ドラッグが二重予約**されている
  （§3.3 の任意加速＝ダイブが同じ操作）。ダイブのほうがゲーム性に効くので、そちらを採った

**まだ手をつけていないもの**

- （なし。§6.3・§3.9 とも 2026-08-21 に実装した）

- 🚨 **本体に戻すとき、`firestore.rules` の変更も一緒に入れること**（下の §3.9 参照）。
  入れ忘れると、スコアが**無言で保存されない**
  - 🐛 **2026-08-21 に穴を見つけて直した**：終わりかたが3つある（①落ちた
    ②月・火星で「ここでやめる」③手紙を読みおえた）のに、**3軸の数字を渡していたのは
    ①だけ**だった。②では score と meters しか送らず、③では何も送っていなかった。
    → `finishRun()` にまとめた。**何度呼ばれても合計が狂わない**ように、
    協力メーターは「前に足したところからの差」を足す方式にした
    （月→続行→落下 で2回通るため。実測 3000→9000 で 9000。12000 にならない）

## 🔐 Firestore のルール（2026-08-21）

### やったこと
1. `gameIdOk` に `^jump3d(_gain|_coop|_w[0-9]{1,6})?$` を足した（3軸ランキング用）
2. **`tetris2Best` と `jump3dBest`** を、`app.js` の `BACKUP_KEYS` と
   `firestore.rules` の `hasOnly` に**両方**足した
3. `js/jump3d-embed.js` が `cj-score` を受けたとき `jump3dBest` を書くようにした
4. `scripts/check_backup_rules.js` に**検査③**を追加した

### 🚨 見つけた抜け（2件とも「無言で失敗する」たぐい）
- **`jump3d` が `gameIdOk` に無かった。** そのまま本体に戻すと `saveGameScore` が
  無言で失敗して、クラウドにスコアが1件も残らない。
  tetris2・jadepanic に続き**3回目**の同じ事故 → `scripts/check_game_rules.js` を作った
- **`tetris2Best` が `BACKUP_KEYS` に無かった。**（2026-08-20 の組込みから抜けていた）
  こちらはルールと食いちがわないので `check_backup_rules.js` の②では見つからない。
  **端末を変えた瞬間に、おとんテトリス2の自己ベストが消えるだけ**で、何も言わない。
  → 検査③「localStorage に書いている〜Best が BACKUP_KEYS に入っているか」を追加。
    わざと1つ抜いて、ちゃんと落ちることも確かめた

### ★足す順番をまちがえない
**ルールを先に広げてから、BACKUP_KEYS に足す。** 逆にすると、そのキーを持つ端末だけ
`hasOnly` が丸ごと拒否して、**その子の記録がすべて 無言で保存されなくなる**。

### まだ人の手が要る
- `firebase deploy --only firestore:rules` は**このPCにCLIが無い**ので流していない。
  本体に戻すときに、本人の環境でデプロイが要る
- かっこの釣り合いと追加ぶんの目視は済み（`{}()[]` の残り0）

## 🪽🌠🪨🐰 プランの取りこぼしを潰した（2026-08-21）

本人の指摘「**プラン完遂できてないよね**」。そのとおりだった。
**つばさは「動き」だけ作って「見た目」を作らないまま『済』と報告していた。**
以後、§を「済」と書くときは**絵で確かめてから**にする。

| やったこと | 中身 |
|---|---|
| 🪽 **滑空のポーズ** | つばさ中は羽ばたかず、翼を大きく開いたまま。前傾＋横へ動くとバンク。出入りは0.15秒でなめらかに混ぜる |
| 🌠 **流れ星**（§6.5） | **高度100kmくらい＝カーマンラインのあたり**に出す（本物が光る高さ）。頭が明るく、しっぽへ消える |
| 🪨 **近景の岩**（§6.1 L4） | 小惑星帯から先、画面の**はし**をゆっくり流れる。★まん中には出さない（見えなくて落ちた、を作らない） |
| 🌌 **場所に入る一瞬だけカメラが引く**（§5.1-6） | チッチが小さくなる＝自分が小さくなる。2秒で戻す |
| 📜 **締めの文言**（§5.7） | 「きょうは、ここまで。宇宙は、まだつづく。」 |
| 🐰 **月のうさぎを消した** | 本人「動いてないし何かわからんので」。2D版にはいたが、この大きさでは もちをつく動きが読めない |
| 🖼 **カードのバナー** | `images/jump3d-banner.png`（1200×280）。`scripts/make_jump3d_banner.py` |

### 踏んだ落とし穴
- 🚨 **`iRot` は Y軸まわりだけ。** 流れ星を π/2 で寝かせたら、しっぽが**画面の奥**を向いて
  ただの丸い光になった。横に寝かせる（すじにする）なら 0 か π
- 🚨 **遠くに小さく置いた流れ星は、青い空では白い点にしかならない。** 近くを大きく走らせる
- 🖼 **バナーは実プレイの画から作れなかった。** カードは横長（4.3:1）だが、ゲームの画角は
  縦 1.16rad で固定なので、横長で撮ると**上下はそのままで横だけ広がる**＝
  チッチが豆つぶで左右が空っぽになる（月・街・富士山・ボイジャーの4案とも撮って確かめた）。
  → 本人のキーアート（`素材/チッチジャンプ3D.png`）から帯を切り出した。
  **カードは左半分にテキストが乗る**ので、CSS のグラデは tetris2 より弱めにすること

## 🌌 いちばん遠くを「観測できる宇宙のはて」にした（2026-08-21・本人の指示）

「表示距離は観測可能な宇宙の端の距離にしてほしい」「盛り込んだ宇宙のギミックは
全て体験できるように」「観測可能な宇宙の端の説明も」「景色の名前も教えてあげると
宇宙に興味わくよね」「最後のエンディングは何度でもみれるように」「月からのスタートも」。

### 距離の階段を のばした
`CJ_ANCHORS` の先を、銀河のあつまりが1段ずつ大きくなる順に足した。数字はぜんぶ本物。

| progress | 場所 | 表示 |
|---|---|---|
| 92,000 | アンドロメダ銀河 | 250万光年 |
| 110,000 | 局部銀河群 | 1,000万光年 |
| 128,000 | おとめ座銀河団 | 6,500万光年 |
| 145,000 | ラニアケア超銀河団 | 5億光年 |
| 160,000 | 宇宙の大きなあみ目 | 10億光年 |
| 175,000 | いちばん遠い銀河 | 134億光年 |
| **190,000** | **観測できる宇宙のはて** | **465億光年**（＋手紙） |

- 🚨 **はてから先は、表示距離を止める。** 勾配を伸ばして 500億・600億光年…と
  出してはいけない。そこから先の光は**まだ届いていない＝見えないので、距離が存在しない**。
  本人の言うとおり「あれ以上遠くは登れる人いなくなる」＝ここがおしまい。
  点は伸びつづけるので、登る理由はなくならない
- はてに着くと、注記が出る：「これより遠くは 光がまだ届いていない」

### 宇宙のはての説明（6行）
むずかしいのは「宇宙は138億才なのに、はては465億光年」。
**光が走っているあいだに、宇宙のほうも広がった**——そこだけ伝える。読みおえると手紙。

### 景色の名前と、そのほんとうのこと
biome ごとに `fact` を1行持たせた（`CJ_BIOME_DEFS`）。名前のポップのあとに静かに出る。
例：ブラックホール＝「重すぎて、光さえ 出られない。」／
パルサー＝「1秒に何回も回る星。灯台のように 光が回ってくる。」
★感想は書かない。「すごい」「きれい」は子ども側の取り分（プラン §5.5）。

### はてまでに、用意した場所を**全部1回ずつ通す**
🚨 **秒数だけで決めていると、登る速さを変えたとたんに取りこぼす。**
深部のペースを上げたら、ブラックホールが出なくなった（実測）。
→ 残りの場所が はてまでに収まるよう、**1つあたりの持ち分で長さに頭を打つ**。
10種の乱数で検査して、**どの種でも11か所ぜんぶ通る**（1つ12〜28秒）。

### かかる時間（自動操縦・落ちても続けた場合）
| スタート | はてまで |
|---|---|
| 🌳 公園から | 約11分 |
| 🔴 火星から | 約8分 |
| 🌙 月から（新）| — |

深部のペースを上げてある（本人「そこまでいくと すごいスピードで飛んでるんだから」）。
★**biome の長さは「秒」で決めてあるので、ペースを上げても
1つあたりの見えている時間は変わらない**（progress の幅が広がるだけ）。

### エンディングは何度でも
プランは「一度きり」だったが、はてまで10分ちかくかかる。見のがしたら二度と読めないし、
兄弟に見せることもできない。→ **着いた人には、いつでも見せる。**

## 🖥 画面チカチカ（2回目・2026-08-21）

本人「**画面チカチカでテストプレイできない**」。1回目（画質判定が毎フレーム動いていた）
の直しでは足りなかった。**残っていた本当の原因はこれ。**

1. 立ちあがりの数フレームは必ず重い → いちど段が下がる
2. 下がると軽くなる → 「12ms未満＝余裕がある」で段が上がる
3. 上がるとまた重い → 下がる …… **1.2秒ごとに canvas を作り直す**

★とくに **120Hz・144Hz のモニタ**で必ず起きる。1フレームが7〜8msなので
「余裕がある」の条件にいつでも当てはまる。**PCでしか出ない。**

**直しかた（振動が起きえない作りにした）**
- 遊んでいるあいだは **下げることしかしない**（`running` のときは上げない）
- 下げるのは **2回続けて重かったとき**だけ
- 始めの3秒は数えない（読みこみの山を拾わない）
- プレイ開始のたび、いちばん上の段に戻す

🚨 **あわせて見つけたバグ**：画質の判定に `dt` を使っていた。`dt` は
`Math.min(実時間, 1/20)` で頭打ちにしてあるので、**重い端末ほど「3秒待つ」が
19秒になる**（実測。swiftshaderで一度も段が下がらなかった）。実時間で数えるよう直した。

★swiftshader は常に重いので、**この環境では振動を一度も再現できない**。
「再現してから直す」ができない種類の不具合。**作りで潰すしかない。**
（検証：22秒の実測で canvas の作り直しは3回＝下げる方向の上限。上げ直しは0回）

## 🪨 岩が薄いのは「厚み」ではなく「奥ゆき」だった（2026-08-22）

本人「宇宙の足場（岩）が薄い。`PLAT_THICK_ROCK` が 4.8 のまま」。
**数字は上げた。が、それだけでは直らなかった。** 実測でこうなっていた。

| | はば(x) | 高さ(y) | **奥ゆき(z)** |
|---|---|---|---|
| 直す前の岩（world） | 0.93 | 1.05 | **0.37** |

＝ **立てた板**。しかも高さのほうが はば より大きい＝すでに「塔」ぎみ。
だから `PLAT_THICK_ROCK` を 7.6 まで上げた絵を撮っても、
**背が高くなるだけで「うすい」は直らなかった**（`tools/rock_ab.py` の d/e で確認）。

★**筒を回りこんだ足場は、横から見ることになる。そのとき目に入るのは
  はば(x) ではなく奥ゆき(z)。** z がうすいと、真横で紙きれになる。

### 直したもの
| | 前 | 後 |
|---|---|---|
| `PLAT_THICK_ROCK` | 4.8 | **5.6** |
| `PLAT_DEPTH_ROCK`（新） | （0.72 決め打ち） | **1.10** |
| `PLAT_THICK_HIGH`（新） | （岩の 0.92倍と書いてあった） | **4.42**（雲は雲で持つ） |

- ★**雲の厚みを岩の定数から作らない。** 前は `PLAT_THICK_ROCK * 0.92` と
  書いてあったので、**岩をいじると雲まで動いた**。切りはなした。
  宇宙に入る所では形（雲→岩）ごと入れかわるので、数字が段でも見えない
- ★**奥ゆきの上限はチッチが決める。** チッチは足場より `CHICCHI_FRONT = 0.24` 手前に立つ。
  岩の前面は `0.371 × (はば × 奥ゆき)`。1.10 で 0.28。
  `tools/rock_stand.py` で、乗ったところを撮って**かぶっていないことを確認ずみ**
- ★**判定はPCの形でもやること。** canvas は `min(100vw, 70vh)`＝**たて0.70の枠**に収まる。
  スマホ（0.46くらい）より横に広いので、同じ足場でも**枠のわりに小さく**見える

### 新しい道具
- `tools/rock.py` … 遊ばせながら、足場の**画面上の幅と高さ(px)**を数える。
  🚨 **筒の横がわに回りこんだ足場は横はばが遠近でつぶれる**（実測で 6px になった）。
  厚みの物差しにならないので、**こちらを向いている足場だけ**数える
- `tools/rock_ab.py` … 数字を変えて**同じ配置・同じ間**で撮りくらべる
- `__cj.setThick(雲, 岩, 奥ゆき)` / `__cj.platBoxes()` / `__cj.thickInfo()` … 走らせたまま測れる口

## 🖥 画面チカチカ（3回目・2026-08-22）— `desynchronized` を切った

本人「チカチカが残っている。画質の自動調整は犯人ではない（診断の作りなおしは起動時の1回だけ）」。
そのとおりだった。**別の原因を2つ見つけた。**

### ① `desynchronized: true`（いちばんの容疑者・これを切った）
`js/gl.js` の `getContext('webgl2', {...})` に入っていた。
これは「低遅延キャンバス」の指定で、**キャンバスをブラウザの画面合成からとびこして**
直に出す道に乗る。速い代わりに

- 描きおわる前の絵がそのまま出る（＝ちぎれる・チカチカする）
- 上に重ねた HUD の字と、絵の出るタイミングがそろわない

**Windows のPC＋高いリフレッシュのモニタ（120/144/165Hz）で出る。**
このゲームは指1本のなぞり操作で、1フレームの遅れが効くたぐいではない＝**捨てても何も失わない**。

- 🚨 **`?desync=1` で前の作りに戻せる。ゲーム中は F4 で入れかえて開きなおす。**
  こちらでチカチカして、ふつうの方でしないなら**それで確定**

### ✅ 実機で直った（2026-08-22・本人「チカチカしないです」）

★ただし **`desynchronized` だけが原因とは言いきれない。**
本人いわく「**テトリス2とジェイドパニックは前からチカチカしない**」。
その2つにも `desynchronized: true` は入ったままなので、
**この指定だけで必ず出るわけではない**。チッチジャンプ3D だけに何かが重なっていた
（毎フレームの `hud.innerHTML` の作り直しは、この3つの中でここだけだった）。

→ **`lab/tetris2` と `lab/jadepanic` は触っていない。出ていないものを直さない。**
　 もし将来この2つでも出たら、まず `desynchronized` を切ってみること。

### ② HUD を毎フレーム作り直していた
`hud.innerHTML = …` が loop の中にあった。**165Hz なら1秒に165回、HUDのDOMを
丸ごと捨てて作り直していた**ことになる。

- `#hud .pop` には `animation: popin .18s` がかかっている。毎フレーム作り直すと
  **アニメが0にもどりつづける＝出るはずの字が出ない**
- 字の下の3Dと、字の出るタイミングがそろわない
→ **中身が変わったときだけ書く**ようにした。`#voyline` と `#strip` も同じ。
  `#strip-me` は `transition: bottom .25s` がかかっているので、0.1%きざみにまるめた。

### 実測で「ちがう」と分かったこと
`tools/flicker.py` を作った。**となり合うコマの差(d1)と、1コマとばしの差(d2)**をくらべる。
チカチカは A→B→A→B なので **d1 が d2 より大きくなる**。

| 場所 | d1 | d2 | d1/d2 |
|---|---|---|---|
| 公園 | 8.99 | 11.15 | 0.81 |
| 空 | 3.15 | 4.60 | 0.68 |
| 小惑星 | 5.18 | 9.10 | 0.57 |
| 深宇宙 | 1.35 | 2.26 | 0.60 |

**どこも 1.0 未満＝「作りが毎フレーム切りかわる」たぐいの明滅は無い。**
＝ 犯人は絵の作りかたではなく、**出しかた（合成）**のほう。①と話が合う。

### 実機で読んでもらう窓（F2 の診断を増やした）
```
size 504x720  作りなおし 1回      ← 増えつづけるなら canvas の作り直し
css  504.00 x 720.00              ← 小数がゆれるならスクロールバーの出入り
q 1  dpr 1.00                     ← 行ったり来たりなら画質の自動調整
fps 165  frame 6.1ms
コマ 5.9〜6.3ms  落ちコマ 0回     ← 落ちコマが増えるなら「明滅」でなく「コマ落ち」
desync OFF(いまの作り)  F4で入れかえ
F3で字を消す:ふつう               ← 消して止まるなら、犯人は重ねた字のほう
```
- **F2** … 診断の出し入れ
- **F3** … 重ねてある字（HUD・帯・ボタン）をぜんぶ消す＝**絵と字のどちらが犯人か**
- **F4** … `desync` を入れかえて開きなおす＝**①が犯人かどうか**

## 🎒🔙 実機で遊んでもらって出た3つ（2026-08-22・本人の指摘）

### ① 道具（🪽🚀）が**無限に使えた**
`core.useItem()` を**直に呼んでいた**。数を見ていないので、押しただけ出る。

- 財布は**本体の `localStorage['items']`**。テトリス・おかんスイーパー・
  チッチジャンプ2 と**共通**で、**勉強のごほうび（ガチャ）でしか増えない**。
  ここだけ無限だと、「勉強すると道具がもらえる」という土台がまるごと効かなくなる
- 2D版（`js/jump.js` の `jumpUseItem`）は `getItems()` → `addItem(kind,-1)` している。同じにした
- 🚨 **iframe の中では減らせない。** 持ち主は本体。`cj-use-item` で親に頼み、
  親が `addItem` して**本当の数を `cj-items` で返す**。ゲーム側は
  押した手ごたえのために先に1つ減らして見せ、返ってきた数で**必ず上書き**する
- 単体で開いたときは同じ鍵をそのまま読み書きする（置き場が同じなのでズレない）
- 効いているあいだは もう1つ使わない（連打で数だけ減らさない。2D版と同じ決まり）
- 実測：3まい→3回で0→ボタンが押せなくなる／iframe ごしでも本体の財布が減る

### ② 道具ボタンを**左**へ
本人「**右手の指でチッチを操作するので 左に置いてた方が遊びやすい**」。
★左のはしには帯（`#strip`・`left+9px`・はば12px）がある。
`left: calc(var(--pad-x) + 26px)` にして、その外がわに置く。
`+10px` にすると、画面が低いときに帯の下のほうの点をボタンが隠す。

### ③ **落ちたあと「‹ もどる」が効かない**
`#overlay` が `position: fixed; inset: 0; z-index: 20` で**画面ぜんぶを覆う**のに、
`#topbar` に z-index が無かった。＝**タイトルと結果の画面では、透明な板が
ボタンの上にかぶさっていた。** 遊んでいる間だけ押せる＝**いちばん押したい
「落ちたあと」だけ死んでいた。**

→ `#topbar { z-index: 24 }`。手紙のあいだは `letterChrome()` が `.hide` を付けて
消す（`pointer-events` も切れる）ので、手紙の上にボタンが浮くことはない。

🚨 **「押せるか」は見た目では分からない。** ボタンの中心で
`document.elementFromPoint()` を呼んで、**返ってくるのがボタン自身か**を見ること。
実測すると タイトル画面では `#title-art` が、結果画面では `#overlay` が上にいた。

## 🌙🔴 月と火星に、本当のことを置いた（2026-08-21・本人の指示）

指示は「**月面にはアメリカ国旗が立っててアポロの説明**」「**火星はキュリオシティと
移住計画**」「**アルテミス計画も盛り込んで**」。

### 置いたもの
| | 月 | 火星 |
|---|---|---|
| モデル | 🇺🇸 **アポロの星条旗**／👣 **足あと**／うさぎ（2D版どおり） | 🤖 キュリオシティ／🏠 **居住ドーム**／宇宙人（2D版どおり） |
| ことば | アポロ（もう行った）→ **アルテミス**（これから行く） | キュリオシティ（いま走っている）→ 人が住む計画 |

- **アポロ＝過去／アルテミス＝これから／火星＝その先**、と順につなげた。
  「アルテミスで月に住めたら、つぎは火星」——ここがつながると、月と火星が別の話でなくなる
- 旗は**上に横の棒**をわたしてある。月には風がないので、これがないと旗はたれる。
  **旗がはためいて見えるのは、この棒のおかげ**——これを見せたくて足あとも置いた
- 着いたら、ボタンより先に1行ずつ出す（さわれば飛ばせる）

🚨 **しま模様を `colFn` で描こうとしてはいけない。** `colFn` は**頂点ごと**に呼ばれる。
箱の面は4頂点しかないので、赤と白があいだで混ざって「にじんだ四角」になる（実際そうなった）。
→ **帯を1本ずつ、別の箱として作る。**

## ✈️ ジャンボとジェット気流（2026-08-21・本人の指示）

「高度10000mぐらいでジャンボジェットがかすめて飛んでいったら格好良い」
「ジェット気流でその付近の高度は風が吹いたら最高」
「もっとディテール細かくして　むこうからこっちに向かって飛んでくるように」

- **場所は決まっていた。** `CJ_ANCHORS` の `[500, 10km, 'ヒコーキの高さ']` がそのまま
  高度1万m。**数字を別に決めない。同じ高さに、同じものを置く。**
- **ジェット気流**（`cjJetWind`）：progress 440〜660。**足場がぜんぶ西→東へ流される。**
  ★チッチを直接おさない。指の位置と体の位置がずれて「バグっぽい」と感じる。
  足場が流れるなら、目で見えるぶん、追いかけるのが技になる
- **ジャンボ**：1プレイ1回だけ。遠くから**こちらへ向かって**来て、頭の上をかすめて抜ける

### 踏んだ落とし穴

- 🚨 **軌道を曲げてはいけない**（本人「近くに来たらドリフトしてバックで飛ぶんよな」）。
  見こみ角で軌道を書いたら、近づいたところで角度の変化が急になり、
  そこから機首を計算していたので**機体がぐるんと回った**。
  ★**本物の飛行機は、すぐそばを通るときもまっすぐ飛んでいる。**
    見かけの角度がぐんぐん変わるのは遠近のせいで、機体は向きを変えない。
  → **3次元の直線**にして、機首の角度は**直線の向き（＝ずっと同じ）**にした。
- 上へ逃がしすぎない。いちばん大きく見える前に画面から出てしまう
  （横 ±0.17rad・上 +0.41rad の画角に、ぎりぎりまで残す寸法にしてある）
- 🚨 **機首の向きを2回まちがえ、2回とも本人に「後ろ向き」と指摘された。**
  「なんとなく飛行機に見える」で判定したのが原因。**見わけかたを先に決めること。**
  ジャンボは**二階席のこぶが機首寄り**にあるので、
  **こぶが垂直尾翼より手前に見えていれば、こちらを向いている。**
- 機首の角度は**実際に進む向きから計算する**（`atan2(奥ゆきの変化, 横の変化)`）。
  手で角度を書くと、軌道を変えたとたんに しっぽから飛ぶ

## 🎛 §6.3 場所ごとの手ざわり（2026-08-21）

じゃま役の出かたと足場の配合を、**高度の直書き分岐ではなく表**（`CJ_ZONES`）で決める。
「その場所らしさ」を数字1つで足せるようになった。深宇宙の巡回ぶんは
`CJ_BIOME_DEFS[key].mods` に持たせてある。

実測（`tools/zone_check.js`。足場を1,200枚つくって数える）:

| 場所 | 氷 | こわれ | 動く | じゃま役 |
|---|---|---|---|---|
| 火星のむこう（基準） | 18.5% | 19.5% | 19.2% | 1.00倍 |
| 小惑星帯 | 9.2% | **35.6%** | 13.8% | 1.30倍 |
| 海王星（猛烈な風） | 19.3% | 20.2% | **25.9%** | 1.20倍 |
| カイパーベルト | **35.6%** | 13.3% | 11.9% | 1.00倍 |
| ヘリオポーズ | 9.6% | 9.4% | 21.3% | **0.15倍** |
| ボイジャー並走 | — | — | — | **0.00倍** |

- ★足場は「**その足場が置かれる高さ**」で表を引く。プレイヤーのいまの進行度で引くと、
  画面の外に先に作ってある足場が1画面ぶんずれる（ごほうびゾーンで踏んだのと同じ罠）
- ★`VOY_SPAN` が core と game に二重に書いてあったので core に一本化した。
  ずれると「絵は並走しているのにタカが飛んでくる」が起きる
- **表を書いただけで満足しない。** `zone_check.js` で配合が本当に変わっていることを数える

## 🏆 §3.9 兄弟ランキングの集計側（2026-08-21）

### 🚨 まず、本番前に事故を1件見つけた

**`firestore.rules` の `gameIdOk` に `jump3d` が入っていなかった。**
このまま本体に戻すと、`saveGameScore('jump3d', …)` が **無言で失敗**して
（try/catch で握りつぶされる）、クラウドに一度もスコアが残らない。
2026-08-20 に tetris2 と jadepanic_* でまったく同じ事故を起こしている。**3回目。**

→ `scripts/check_game_rules.js` を作った。
**コードが保存しているゲームIDが rules で許可されているかを機械で数える。**
ゲームを増やしたら必ず走らせる。
（第1引数を関数で作っている呼び出しは追えないので、その場所は一覧に出して人に投げる）

### 3軸の持たせかた

rules が `keys().hasOnly(['value','lastUpdated'])` で縛っているので、
**1つの記録に3つの数字は入れられない**。→ **ゲームIDを分ける**。

| ゲームID | 中身 |
|---|---|
| `jump3d` | 生スコア |
| `jump3d_w<週番号>` | 今週のシードチャレンジ（週がわりで別の表になる） |
| `jump3d_gain` | 自己ベストの更新幅（**伸びしろ＝弟が勝てる軸**） |
| `jump3d_coop` | 協力メーター（合計距離） |

rules 側は `^jump3d(_gain|_coop|_w[0-9]{1,6})?$` を1行足した。

## 🏙 序盤の景色をやり直した（2026-08-21・本人の指示）

指示は3つ。「**富士山に沈む太陽はいらない**」「**富士山をもう少し大きく作り直して**」
「**遠景のビル群はもっと増やして**」。

### ☀️ 夕日はやめた
序盤の主役は「街のあいだを登る」ことと、その向こうの富士山。夕日を足すとそこへ目が行く。

### 🗻 富士山
**実物どおりでは、この画面では絶対に大きくならない。**
本物は 3776m・100km 先＝見こみ角 0.038rad。縦の画角は 1.16rad なので画面の高さの3%。
しかも横は 0.38rad で、**横の画角（0.34rad）より広い**。
＝「横は画面からはみ出すのに、縦はぺったんこ」→ **平たい灰色のくさび**にしかならない。

- → 距離を 100km→**36km**、裾を 38km→**16km**（＝斜面を実物より急に）。
  見こみ角は 0.038→**0.105rad**（画面の高さの9%）。**見なれた「富士山の絵」は
  写真より急な山**なので、そちらに合わせるのが正解
- メッシュも作り直し：分割 22×26→**34×44**／**山頂を一点にしない**（本物は直径700mの
  火口があって平らに見える。とがらせると「とんがり帽子」）／**宝永山**のこぶを足した
  （富士山が左右対称でないのはこれのおかげ。あるだけで「絵の山」でなくなる）／
  雪の線を尾根なりに**ゆらす**（まっすぐ切ると「白い帽子をかぶせた円すい」になる）
- `lateral` は **＋（右）**へ。チッチは登るほど画面の左へ寄る（climb.py で x が 0.00→-0.72）ので、
  左に置くと山頂がずっと隠れる

### 🏙 ビル群 72棟 → 620棟

そのまま増やすと**3倍重くなった**（実測 107ms→321ms）。3つ直した。

1. **画面の外のビルを6割ちかく描いていた。** `latPx` は ±0.41rad まで返すのに、
   横の画角は ±0.17rad しかない。`|lat| > 0.22` で切る
2. **遠くのビルにも 124段のメッシュを使っていた。** 620棟で三角が120万枚。
   遠景用の軽いメッシュ（6段）を用意した。
   🚨 **選ぶ基準を `hw`（その奥ゆきでの world の大きさ）にしてはいけない。**
   depth は距離とともに増えるので、遠くなっても hw はほとんど小さくならない。
   **見こみ角（実物の高さ ÷ 実きょり）**で決めること
3. **数pxのビルに輪郭線を描いていた**（ジオメトリが倍）。`opt.noOutline` を足した
4. あわせて **同じメッシュが続くところをまとめて描く**ようにした
   （前は `scnList` を1つずつ `drawInstances` していた＝数百回の描画呼び出し）

結果：**113ms / 79ms**（元は 107ms / 67ms）。8.6倍のビルで +6%／+18%。
★swiftshader＝CPU描画なのでブレが大きい。**中央値でなく「3回まわして最小」で比べること**
（同じ絵で 77〜137ms のブレが出て、一度まちがった結論を出した）。

- 増やすのは**遠景だけ**。手前のビルは画面をいっぱいに覆うので、数がそのまま重さになる
- **遠いビルは暗くではなく、もやで明るく青くする。** 暗くすると地平線に黒い棒が
  ならんだように見えて街に見えない。★色は「頂点の色 × 渡す色」なので、
  空の色をそのまま渡すと暗くなる。1.8倍くらいにしてやっと空になじむ

## ⏱ 登る速さを測って、biome の長さを決めなおした（2026-08-21）

**新しい道具：`tools/pace_check.js`**（自動操縦で 5回まわして、いちばん進んだ回を採る）。

| progress | 到達 | その区間の速さ |
|---|---|---|
| 3,000（月） | 2分02秒 | 26/秒 |
| 5,000（火星） | 3分27秒 | 24/秒 |
| 15,000（ボイジャー） | 4分28秒 | 182/秒 |
| 30,000（巡回の始まり） | **5分22秒** | 250/秒 |

- 🚨 **監修の「巡回の始まり30,000は15分かかる」は、ペース調整**前**の話だった。**
  実測 5分22秒＝**届く**。直す必要はなかった（測らずに直していたら改悪していた）
- 🚨 **biome 1つが 1.7〜3.1 秒しかもっていなかった**（監修の「5〜9秒」よりさらに深刻）。
  原因＝**長さを progress で決め打ちしていた**こと。深いところは1秒に 600〜700 progress
  進むので、1,200〜2,200 progress では2秒で終わる。
  → **「何秒もつか」で決めるように変えた**（`CJ_BIOME_SEC_MIN/MAX = 45〜80秒`）。
  切りかわりの混ぜ幅（`fade`）も区間ごとに持たせた（固定250だと深部で0.3秒＝一瞬）
- 実測しなおして **27.4〜79.2秒**（`biome_check.js` に検査を追加）
- ★**名前つきの場所（オリオン等）は 15,000 progress おき＝深部では約45秒しかない。**
  55秒で作るとオリオン大星雲が天の川の中心を丸ごと飲みこむ（NG 15件）。次の場所の
  手前で必ず切るようにした

## 🎮 プランの残りを実装（2026-08-21・本人「どんどん進めてください」）

| プラン | やったこと |
|---|---|
| §3.2 **こわれ雲が下を巻きこむ** | こわれた足場が**落ちていき**、真下の足場を道づれにする。実測 **2.1回/分**。これが「**下を見る理由**」 |
| §3.2 **つばさ＝滑空** | 滑空中は塔を**回りこむ速さが上がる**（デッドゾーン0.16→0.06、追従3→8）。外周と内側を乗りかえる道具になる。★操作は指1本のまま |
| §3.6 **落ちたあとのエンディング** | 7.6秒。4.2倍まで引いて、**来た道を45秒ぶん巻きもどして**から到達点へ戻り、場所の名まえを出す |
| §3.6 **復活** | 1ランに1回・**遊び券は取らない**・代償は「これから増える点が半分」。ここまでの点は消さない |
| §3.7 **自己ベストのゴースト** | 前のいちばん良かった回の「その時刻の高さ」に**金の線**が並走。抜くと1回だけ光る |
| §4.4 **数字のルーレット** | 桁が変わる瞬間だけ数字が回って、**左の桁から順に**止まる＋カメラが1.12倍引く |

### ここで踏んだ落とし穴

- 🚨 **エンディングで「来た道を 0 まで巻きもどす」をやってはいけない。**
  深宇宙は1秒に600progress進むので、全部戻すと biome がコマ送りで切りかわって
  **画面が明滅する**（子ども向けの2Hz以下に違反）。戻すのは**45秒ぶんだけ**にして
  2.5秒かける＝0.4Hz
- ゴーストを**鳥の姿で出さない**。2匹目のチッチ（§5.6）と見わけがつかなくなる → 線にした
- ゴーストが画面に映るのは**±42progress の間だけ**（画面が見せている高さがそれだけ）。
  検証で 2560 と 2500 を比べて「出ない」と勘ちがいした。**世界の高さで考えること**

## ☄️ 彗星（2026-08-21・本人「彗星はやろうよ」）

**空ではなく「もの」として作った。** 彗星は動くので、背景シェーダ（`skyFS`）では表せない。
惑星と同じインスタンスで置き、**加算合成**でにじませる。

- `buildCometMesh(lean)`（`scenery.js`）。しっぽのかたむき違いを3つ焼く
  （`iRot` は **Y軸まわりしか回せない**ので、画面の中での傾きはメッシュに焼くしかない）
- **コマ**＝大きさのちがう球を3枚重ねる。加算なので**重なった枚数がそのまま明るさ**＝にじみになる。
  色が緑白なのは、本物のコマが C₂ という分子で光るから
- **しっぽは2本**。イオンの尾＝まっすぐ・細い・青／ダストの尾＝太く短く・**カーブして遅れる**・黄白
- 出る場所は3つ：**カイパーベルト(11,300〜12,700)／オールトの雲(20,000〜30,000・薄め)／
  巡回の `comet` バイオーム**。★深宇宙の巡回だけに置くと**ふつうに遊んで一度も見られない**。
  それに、彗星が来るのは本当にカイパーとオールト＝事実どおりに置けば届く場所にも出せる
- 入るときは長く（遠くから近づく）、出るときは短く。**13,000 の手前で必ず切る**
  （そこからはヘリオポーズ＝見せ場の前の静けさで、何も出してはいけない）
- 事実を1行だけ置く：「**しっぽは、進む向きではなく、太陽と反対がわへのびる。**」感想は書かない

### 実測でわかった落とし穴

- **`additive` だけではダメで、トゥーン陰影がかかって「黒い円すい」になる。**
  彗星は自分で光っているので、陰影を計算してはいけない。→ `uGlass = 3`（自発光）を新設。
  中身は `vCol * max(dot(N,V),0)`＝**正面ほど明るく、ふちほど暗い**。
  球なら「まん中が明るい玉」、円すいなら「ふちのやわらかいしっぽ」になる
- **`bgPlace` の横のずれ `lat` は ±0.15 まで。** 横の画角が ±0.17rad しかないので、
  0.24 にすると**一度も画面に映らない**（実測でそうなった）
- **2本の尾の角度をしっかり離す**（0.44rad）。近いと明るいイオンの尾に飲まれて「尾は1本」に見える
- 描画コストは測定誤差の範囲（彗星あり 53.2ms／なし 54.8ms・swiftshader）

## 🪨 宇宙の足場のモデリング（2026-08-21）

本人の指示「**宇宙の足場もモデリングしよ**」。4種を作り分けた。

| 種類 | 見わけの決め手 |
|---|---|
| 岩 | **クレーター**（おわん＋ふちの盛りあがり）／上のめんを少し平らに／上に小石3つ |
| 氷 | **六角柱**の結晶を6本（根もとだけ埋める）。三角のトゲでは岩と見わけがつかなかった |
| こわれ | かたまりを広げる＋**暗いくさび**を3本差しこむ（＝ひび） |
| バネ | 平たい岩＋金属のらせん。`springParts(b, null, {dim:0.62})` |

### 実測でわかった落とし穴

- **かたまりを離すと「割れた破片の散らばり」に見える。** すきまを空けず**重ねる**こと。
  `ASTEROID_CHUNKS` を内側へ寄せて、それぞれを大きくした
- **`uFloor` を上げすぎると陰影が全部つぶれて、岩ではなく白い紙きれに見える。**
  0.82 → **0.45**（0.82／0.45／0.0 の3枚を撮って比べた）。背景から浮かせるのと
  形を見せるのは**両立する。ただし 0.5 が上限**
- **粗くすればするほど結晶らしくなる、はウソ。** 氷の土台を `sphereGeometry(5,3)` まで
  粗くしたら「割れたガラスの破片」になって、乗れる足場に見えなくなった → `(8,6)`
- **バネのらせんを 2.1倍のまま宇宙で描くと、色が振り切れてのっぺりした塊になる。**
  `dim` で落とすと、陰影＝らせんの形が出る

## 🪖 「黒いかぶりもの」の正体（2026-08-21・4回目）

ヘルメットが黒いカツラに見える件、原因は**合成でも裏返りでもなかった**。

- 合成は正しく加算（実測：`BLEND_SRC_RGB=SRC_ALPHA, DST=ONE`）＝**暗くはできない**
- 本当の原因は **ドームが頭より大きすぎた**こと。頭の上に空いた分だけ
  fresnel のうすい灰色が広がり、小さく表示したときに「黒い塊」に見えていた
- 直し方：`R_DOME` 0.44 → **0.385**、中心 `+0.20` → **`+0.145`**（頭にぴったり寄せる）＋
  ハイライトを広く（pow 48 → 26、強さ 1.05 → 1.35）

★ここで2回まちがえた。記録しておく。
1. **スクショ2枚を別の瞬間で撮って比べた**（チッチが歩いている最中だった）。
   位置がちがう画像どうしの画素差は**まったく意味がない**
2. `sky.py` が**月の到着演出を消さずに**深宇宙を撮っていた。
   月の地面とウサギを載せたまま「暗黒星雲」として撮れていた（sky.py を修正ずみ）

## 🐤 チッチの明るさの床（2026-08-21）

太陽から離れるほど暗くする（逆二乗）のは正しいが、**チッチまで暗くすると
頭のてっぺんが黒くつぶれる**。プラン §9「プレイヤーはいつも画面でいちばん明るい」に
合わせて、`toonFS` に `uCFloor`（0.66）を入れた。自発光（`uEmis`）だけでは足りない。

## 実測でわかっていること（2026-08-21）

- **物理は fps に左右されない**（`physics_check.js`）。
  120/60/30/20fps どれでもジャンプの高さ 3.9139 world（ちらばり 0.00%）。
  どの落下速度でも足場のすり抜け0。★プランが「最大の落とし穴」と書いた項目
- **道のりの機械検査**（`biome_check.js`）：すきま/重なり0・連続重複0・
  値がすべて有限・向きの長さ1・明滅≤2Hz・1歩の変化 最大0.006
- **子ども向け安全**：全画面の白フラッシュは**存在しない**（単位が上がる演出は
  字のアニメだけ。被弾の白はチッチ本体のみ）。明滅はパルサー1.2Hz／ナビ0.9Hz／
  ロゴ0.29Hz。**すべて2Hz以下**
- 撮った絵：`_climb`13枚・`_sky`17枚＋各シーン。**コンソールエラー0**

## 注意（2026-08-22 更新）

**本体への組込みは終わって、push ずみ**（`f1b05c2` まで）。`.gitignore` の3行は外れ、
`index.html` と `sw.js` にも戻っている。Firestore のルールもデプロイずみ（本人が実施）。

- `aba11b6` 本体に組みこむ
- `9e70531` 岩の厚み・奥ゆき
- `49600c2` チカチカ（`desynchronized` を切る／HUDを毎フレーム作り直さない）
- `f1b05c2` バージョンアップ情報（v589・v590）＋ `sw.js` CACHE_NAME v590

🚨 **`aba11b6` は `data/updates.json` にお知らせを入れ忘れていた**
（新しいゲームが増えたのに、子どもたちには何も伝わらない状態だった）。
`f1b05c2` でさかのぼって足した。**ゲームを増やしたら `data/updates.json` と
`sw.js` の CACHE_NAME を必ずセットで直す。**

### 実機で見てもらった結果（2026-08-22）
1. 🖥 チカチカ … ✅ **直った**（本人「チカチカしないです」）
2. 🪨 岩の厚み・奥ゆき … 絵で確認ずみ
3. そのとき出た3つ（道具が無限／道具の位置／落ちたあと戻れない）… ✅ 直した。
   **上の「実機で遊んでもらって出た3つ」を見ること**

★ファイルを直したら `lab/chicchi-jump-3d/index.html` の `?v=` を上げる（いま `20260821f`）。
  上げないとキャッシュで古いのが出て、直したのに直らないように見える。
★あわせて `sw.js` の CACHE_NAME も上げる（上げないと古いのがキャッシュから出る）。

## 📖🏅🚩 タイトルの2枚と、ほかの子の旗（2026-08-22・本人の指示）

指示は3つ。「タイトル画面に**操作説明**と**スタンプ帳**」「ゲームの雰囲気にあった恰好いいデザイン」
「**ほかのプレーヤーの旗**が表示されるのもやろう」。

### 足したもの
| どこ | 何 |
|---|---|
| `index.html` | タイトルに `.title-menu`（🎮あそびかた／🏅スタンプ帳＋`9/28`のバッジ）・`#sheet`・`#flags` |
| `style.css` | シート一式（角のブラケット／金の罫／`.stamp-grid`）と `.flag` |
| `js/game.js` | `helpHTML()` `stampHTML()` `openSheet()` ／ `CJ_STAMP_ICON` `CJ_STAMP_LIST` ／ 🚩 `setFlagData` `rebuildFlags` `updateFlags` `cjFlagY` |
| `js/jump3d-embed.js` | `jump3d_reach` の保存と `cj-flags-request` → `cj-flags-data` |
| `firestore.rules` | `^jump3d(_gain|_coop|_reach|_w[0-9]{1,6})?$`（**_reach を足した**） |
| `sw.js` | CACHE_NAME v591→v592 ／ `index.html` の `?v=` を 20260822b へ |
| `tools/ui.py` | 🆕 実測スクリプト（20項目。絵も7枚撮る） |

### スタンプ帳
- **溜まる仕組みは前からあった。見るところが無かっただけ。**
  押されるのは `CJ_ANCHORS` の名前つき28か所。出ていたのは
  「そのプレイで新しく押した1個」と「始める場所の3つ」だけで、残り**25個は死蔵**だった。
- まだのところは **`？？？？` で枠だけ**見せる（空き枠が次の理由になる）。
  いちばん遠くまで押せているものに `.tip`（水色の輪）を付けて最前線を出す。
- 保存先は前と同じ `cj:<名前>:stamps`。**作った瞬間に、今まで遊んだぶんが埋まった状態で出る。**

### 🚩 ほかの子の旗
- 出どころは Firestore の **`jump3d_reach`**（＝ progress。**score ではない**）。
  点は⭐やボーナスで増えるので、点の順に旗を立てると
  「点は高いのに、そこまで登っていない」旗が道に立つ。
- 自分＝白の実線 🏳／ほかの子＝水色の破線 🚩／こえたら金。
  ゴースト（金の線・右寄せ）と見わけるため、旗は**左寄せ**。
- **画面に映る高さに来たときだけ出す。** ずっと先の旗は、そこへ自分で登るまで一度も見えない
  （プラン §3.9「絶望的な差を正面から見せない」）。抜けなくても何も言わない。
- 途中から始めたときは、その下の旗を**最初から「こえたこと」に**しておく（通っていないのに祝わない）。

### 🚨 ここで見つけた不具合（2件とも実測でしか出ない）
1. **`cjYAtProgress` をそのまま線に使うと、画面の46%ぶん下に出る。**
   あれが返すのは「カメラの基準の高さ」で `CJ_SCROLL_OFF`（＝公園ぶんのズレ `CJ_PARK_CAMY` 込み）が入っている。
   実測：progress がちょうど旗の数のとき **線 83% ／ チッチ 43%**＝**ずれ 4.50／画面9.69**。
   → `cjFlagY(p)` を作って `climb + CJ_VIEW_H*(1-CJ_SCROLL_FRAC)` にそろえた（ずれ 0.000）。
   ★**同じズレが「きのうのチッチ」の線にもあった**ので、そちらも `cjFlagY` に直した。
2. **`display:none` のあいだに `scrollTop = 0` を書いても効かない。**
   前に開いたシートの位置が残り、スタンプ帳が「いきなり ？？？？ の途中から」開いた。
   → `classList.add('show')` の**あと**で戻す。

### 実測の落とし穴（tools/ui.py を書きながら踏んだ）
- **飛ばす前に到着演出を解く。** `c.over=false; c.ending=null` を付けずに `warpP` すると
  月・火星のイベントで `running=false` になり、「旗が出ない・こえない」が**ゲームのせいに見える**。
- **`step()` を手で回しながら `wait_for_timeout(16)` を挟むと rAF が飢える。** ふつうに待つ。
- **線とチッチの位置は「画面の%」でくらべない。** チッチは足場のあいだで大きく上下するので、
  直っていても 8% ずれて NG になる。**世界の高さ**でくらべる（`flagY(p)` と `player.y`）。
- `document.querySelector('.flag')` は**自分の標**を拾う（先に足しているため）。`:not(.me)` を付ける。

### 確かめかた
```
cd "C:\Users\User\Desktop\Claude\tuna app"
python -m http.server 8899
python lab/chicchi-jump-3d/tools/ui.py     # 20項目ぜんぶ OK ／ コンソール0件
python lab/chicchi-jump-3d/tools/play.py   # 既存の回帰も errors 0
node scripts/check_game_rules.js           # jump3d_reach が許可されているか
node scripts/check_sw_assets.js
```

### 🚨 まだ人の手が要る
- **`firebase deploy --only firestore:rules`**（このPCにCLIが無い）。
  忘れると `jump3d_reach` が**無言で保存されず、旗が永久に立たない**。
- **スタンプはクラウドに載っていない。** `cj:<名前>:stamps` は端末の中だけで、
  `BACKUP_KEYS` は固定キーしか載せられない（`app.js` 1719行）。
  端末を変えると**帳がまるごと空になる**。直すなら「iframe → 本体へ渡して `jump3dStamps` として保存」。
  ★`BACKUP_KEYS` と `firestore.rules` の `hasOnly` は**同時に**足すこと（片方だけだと丸ごと拒否）。
