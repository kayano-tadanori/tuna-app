# -*- coding: utf-8 -*-
"""docs/_audit/g5m_w3/findings_1.md の指摘（小5マスター算数 kokai No.9〜12・kokai No.1(610回)・
2nd演習 fukushu No.19〜21・1〜5、37本監査）を直す修正パッチ。

対象4件:
  hd_5m_k01_610_6 (HG-2608) ... 立方体を斜めに切った柱の体積。
    重大: 原簿は①②③の3設問（③はBR＝1.35cmを求める、作問メモで「本問の白眉」と
      明記された、①②で持たせた道具を逆向きに使わせる最重要設問）。アプリは①②の
      2steps しか実装されておらず、③がまるごと欠落していた。③を追加する
      （新しい図2の設定＝辺BF上に点R・辺CG上に点S・BR＝CSをsteps[2]の設問文で
      導入し、点C側の体積12t・点E側の体積108−6tの比6:37からt＝1.35を求めさせる）。
      steps が2個→3個に増える。

  hd5m2nd_3_1 (HG-0504) ... 望遠鏡積（telescoping）。
    重大: steps[1]の設問文が「4個目までかけると 1/27 になります」と書いていたが、
      実際に 1/5×3/7×5/9×7/11 を計算すると 1/33 であり（telescoping公式
      3/((2n+1)(2n+3)) でn=4のときも 3/99=1/33 と一致）、1/27 は誤り
      （(2n+1)^2 と誤って2乗した場合の値 3/81=1/27 に一致することから、算出過程の
      取り違えと推定）。1/33 に訂正する。steps[1]の答え(3/143、5個目の値)は
      この誤った前提を使わずに求まる式なので影響なし。

  hd5m2nd_21_3 (HG-2022) ... 38人が6人ずつ・日曜を除いて当番、二重周期。
    重大: steps[5]（6問目）で「その次の20日目に、また1〜6番にもどります」と
      正しく20日目が答えだと確定させたのに、steps[6]（7問目、最終問）の解説が
      「①19日目は5月4日です」と、直前のsteps[5]の結論と矛盾する日数（19日目）を
      書いていた。実際にカレンダーを4月12日(月)から日曜(4/18,4/25,5/2)を
      とばして数えると、19日目＝5月3日(月)、20日目＝5月4日(火)であり、
      5月4日に対応するのは20日目（19日目ではない）。原簿(HG-2022)自身にも
      「⚠2026-08-01訂正：ここは元々『19日目で1-6に戻る』と書いていたが……
      答えの5/4（火）は正しく、日目の数え方だけが1ずれていた」という訂正履歴が
      あり、アプリのこの1文だけがその訂正前の言い回しのまま残っていた。
      「19日目は」を「20日目は」に直す（答え自体の4は変更なし）。

  hd5m2nd_1_1 (HG-0501) ... 入れかえた2けたの差は9の倍数。
    軽微: steps[2]（3問目、「あてはまる2けたの整数ABは何通りありますか」→4）の
      解説が「③答えは4通り（61・72・83・94）です」と、4つの整数を全部
      書き出していた。この94は次のsteps[3]（4問目、「そのうちいちばん大きい
      整数はいくつですか」→94）の答えそのものであり、3問目を答えた時点で
      4問目の答えが見えてしまう（「分割した設問の答えを先出し」の頻出パターン）。
      steps[2]の解説から「（61・72・83・94）」の列挙だけを削り、
      「③だから答えは4通りです。」に差し替える（(A,B)の組の列挙は残す＝
      4通りという数え方の根拠は維持される）。

見送り（本パッチに含めない）:
  ・原本(PDF)でしか確定できない指摘は無かった（Google Driveが未接続のため
    確認不可だったが、独立検算・機械抽出ですべて確定できた）。
  ・hd5m2nd_19_1 (HG-2016、食塩水20問) はアプリが8問を抜粋実装。
    hd5m2nd_19_2 (HG-0761、混合4衣装) はアプリが順算2つのみ実装し、
    逆算2つ（濃度が未知／重さが未知）を省略。どちらも「1枚まるごと」
    「1骨を複数衣装」の2nd演習プリントの意図的な抜粋実装であり、他の
    週の同種プリントでも同じ抜粋方針が採られている（新しい発見を1つに
    絞った公開テストの大問とは性質が違う）ため、見送り。
  ・hd5m2nd_21_3 (HG-2022) は、genboの(2)の答えが「5月4日火曜日」（日付＋曜日）
    なのに対し、アプリのsteps[6]は日付（5月4日の「4」）だけを問い、曜日
    （火曜日）を問うstepが無い。(1)側は日付stepと曜日stepの両方があるのに
    (2)側は曜日stepが無く非対称ではあるが、この曜日を求める作業はsteps[4]
    （1問目の日付から曜日を求める）と全く同じ手順の繰り返しであり、
    HG-2608のように問題の核心（本問の白眉）が丸ごと欠けているケースとは
    性質が異なるため、見送り（findingsに記録のみ）。
  ・audit_1.txt中、hd_5m_k01_610_6・hd5m2nd_20_9 の原簿引用ブロックの直後に、
    無関係な原簿見出し（第622回 小3公開・小52nd演習No.21日暦算）が紛れこんで
    いたが、これは監査資料の抽出ツールのノイズであり、アプリ側の実装内容には
    影響しない。
  ・入力形式（answerable format）を変える修正は無し。

使い方:
  python scripts/_fix_g5m_w3_1.py [対象JSONのパス（省略時 data/hama_daimon.json）]

1プロセス内で読み書きし、書き出しは io.open(path, "wb")。json.dumps は indent=1 固定。
大問は genbo_common.iter_daimon だけで引く。置換前に「その大問の中でちょうど1回」を assert。
欄まるごとの一致で冪等性を判定する（2回流しても結果は変わらない）。
"""
import io
import json
import os
import sys

HERE = os.path.dirname(os.path.abspath(__file__))
BASE = os.path.dirname(HERE)
sys.path.insert(0, HERE)
from genbo_common import iter_daimon  # noqa: E402

DEFAULT_PATH = os.path.join(BASE, "data", "hama_daimon.json")


# ============================================================================
# hd_5m_k01_610_6 (HG-2608) ... ③（BR＝1.35cm、本問の白眉）が丸ごと欠落
# ============================================================================
ID_610_6 = "hd_5m_k01_610_6"

OLD_STEP1_610_6 = {
    "question": u"この立体（A,B,C,D,P,Qを頂点とする立体）の体積は何cm³ですか。",
    "answer": u"54",
    "meaning": u"BP＝CQ＝3cmで、辺AB・辺DCはどちらも辺ADに垂直、辺AD・辺BC・辺PQはすべて同じ向き・"
               u"同じ長さ（6cm）です。だから、この立体は「三角形ABP」を底面、辺ADの長さ6cmを高さと"
               u"する三角柱（三角形ABPをそのままAD方向に6cmスライドさせた形）になります。三角形ABPは、"
               u"直角をはさむ2辺が6cmと3cmの直角三角形なので、面積は6×3÷2＝9cm²。9×6＝54cm³。",
}
OLD_STEP2_610_6 = {
    "question": u"この立体を、3点B,P,Dを通る平面で切り分けました。頂点Cをふくむほうの立体の体積は"
                u"何cm³ですか。",
    "answer": u"36",
    "meaning": u"三角柱を、底面の1辺BPと反対側の頂点Dを通る平面で切ると、頂点Aをふくむ側は三角すいABPD"
               u"（体積＝9×6÷3＝18cm³）になります。のこりのCをふくむ側は、54−18＝36cm³。",
}
NEW_STEP3_610_6 = {
    "question": u"同じ立方体で、こんどは 辺BF上に点R、辺CG上に点S を、BR＝CS となるようにとります"
                u"（長さは まだ 分かりません）。この立方体を、3点A,R,Sを通る平面と、3点B,F,Dを通る"
                u"平面の 2つで 切り分けたとき、頂点Cを ふくむ立体と、頂点Eを ふくむ立体の 体積の比が "
                u"6：37 に なりました。BRの 長さは 何cm ですか。",
    "answer": u"1.35",
    "meaning": u"①平面BFDは、立方体をB・Fを通る辺と 頂点Dを通って ちょうど半分に割る面です。"
               u"②平面ARSは、BR＝CS＝t と おくと、高さが t に比例して ななめに切れる面に なります。"
               u"③このとき、頂点Cを ふくむ立体の体積は 12×t、頂点Eを ふくむ立体の体積は 108－6×t に "
               u"なります（①②で持った「柱・すいの体積」の考え方を、こんどは 逆向きに使います）。"
               u"④12×t：(108－6×t)＝6：37 を解くと、444×t＝648－36×t → 480×t＝648 → **t＝1.35cm**です。"
               u"この③が この問題の いちばんの見せ場です。",
}


def fix_610_6(x, fixed, seen):
    if x.get("id") != ID_610_6:
        return
    seen.add(ID_610_6)
    assert x.get("src") == "HG-2608", u"%s: src が想定と異なります (%r)" % (ID_610_6, x.get("src"))

    steps = x.get("steps") or []
    if len(steps) == 3:
        assert steps[2] == NEW_STEP3_610_6, (
            u"%s: steps[2] が想定と異なります（別セッションが書いた可能性）" % ID_610_6
        )
        return  # 冪等：既に直っている

    assert len(steps) == 2, u"%s: steps が想定(2個)と異なります(%d個)" % (ID_610_6, len(steps))
    assert steps[0] == OLD_STEP1_610_6, (
        u"%s: steps[0] が想定と異なります（別セッションが書いた可能性）" % ID_610_6
    )
    assert steps[1] == OLD_STEP2_610_6, (
        u"%s: steps[1] が想定と異なります（別セッションが書いた可能性）" % ID_610_6
    )
    x["steps"] = steps + [NEW_STEP3_610_6]
    fixed.append(u"%s(HG-2608): ③（BR＝1.35cm。原簿が『本問の白眉』と明記した設問）が丸ごと"
                 u"欠落していたのを追加（steps 2個→3個）" % ID_610_6)


# ============================================================================
# hd5m2nd_3_1 (HG-0504) ... 「4個目までかけると1/27になります」が誤り（正しくは1/33）
# ============================================================================
ID_3_1 = "hd5m2nd_3_1"
OLD_Q1_3_1 = u"4個目までかけると 1/27 になります。5個目までかけるといくつですか。分数で答えなさい。"
NEW_Q1_3_1 = u"4個目までかけると 1/33 になります。5個目までかけるといくつですか。分数で答えなさい。"


def fix_3_1(x, fixed, seen):
    if x.get("id") != ID_3_1:
        return
    seen.add(ID_3_1)
    assert x.get("hg") == "HG-0504", u"%s: hg が想定と異なります (%r)" % (ID_3_1, x.get("hg"))

    steps = x.get("steps") or []
    assert len(steps) == 4, u"%s: steps が想定(4個)と異なります(%d個)" % (ID_3_1, len(steps))
    s1 = steps[1]
    if s1.get("question") == NEW_Q1_3_1:
        return  # 冪等
    assert s1.get("question") == OLD_Q1_3_1, (
        u"%s steps[1]: question が想定と異なります（別セッションが書いた可能性）" % ID_3_1
    )
    assert s1.get("answer") == u"3/143", u"%s steps[1]: answer が想定と異なります" % ID_3_1
    s1["question"] = NEW_Q1_3_1
    fixed.append(u"%s(HG-0504): steps[1]の設問文『4個目までかけると1/27になります』が誤りだった"
                 u"（1×3÷(9×11)＝1/33が正しい）のを1/33に訂正" % ID_3_1)


# ============================================================================
# hd5m2nd_21_3 (HG-2022) ... 「19日目は5月4日」が直前のsteps[5]の結論(20日目)と矛盾
# ============================================================================
ID_21_3 = "hd5m2nd_21_3"
OLD_M6_21_3 = (
    u"①19日目は**5月4日**です。②19は7の倍数ではないので、曜日は毎回ずれます。"
    u"③「祝日も当番がある」という一文は**まったく使いません**（使わない条件をわざと置いてあります）。"
)
NEW_M6_21_3 = (
    u"①**20日目**は**5月4日**です。②19は7の倍数ではないので、曜日は毎回ずれます。"
    u"③「祝日も当番がある」という一文は**まったく使いません**（使わない条件をわざと置いてあります）。"
)


def fix_21_3(x, fixed, seen):
    if x.get("id") != ID_21_3:
        return
    seen.add(ID_21_3)
    assert x.get("hg") == "HG-2022", u"%s: hg が想定と異なります (%r)" % (ID_21_3, x.get("hg"))

    steps = x.get("steps") or []
    assert len(steps) == 7, u"%s: steps が想定(7個)と異なります(%d個)" % (ID_21_3, len(steps))
    s6 = steps[6]
    if s6.get("meaning") == NEW_M6_21_3:
        return  # 冪等
    assert s6.get("answer") == u"4", u"%s steps[6]: answer が想定と異なります" % ID_21_3
    assert s6.get("meaning") == OLD_M6_21_3, (
        u"%s steps[6]: meaning が想定と異なります（別セッションが書いた可能性）" % ID_21_3
    )
    s6["meaning"] = NEW_M6_21_3
    fixed.append(u"%s(HG-2022): steps[6]の解説『19日目は5月4日です』が、直前のsteps[5]の結論"
                 u"『その次の20日目にもどります』と矛盾していたのを『20日目は5月4日です』に訂正"
                 u"（実際のカレンダーでも19日目=5月3日・20日目=5月4日）" % ID_21_3)


# ============================================================================
# hd5m2nd_1_1 (HG-0501) ... steps[2]の解説がsteps[3]の答え(94)を先出し
# ============================================================================
ID_1_1 = "hd5m2nd_1_1"
OLD_M2_1_1 = (
    u"①差が5になる (A,B)は (6,1)(7,2)(8,3)(9,4)。②**BAも2けたなのでBは0にできません。**"
    u"Bを0にすると50が入って5通りになってしまいます。③答えは4通り（61・72・83・94）です。"
)
NEW_M2_1_1 = (
    u"①差が5になる (A,B)は (6,1)(7,2)(8,3)(9,4)。②**BAも2けたなのでBは0にできません。**"
    u"Bを0にすると50が入って5通りになってしまいます。③だから答えは4通りです。"
)


def fix_1_1(x, fixed, seen):
    if x.get("id") != ID_1_1:
        return
    seen.add(ID_1_1)
    assert x.get("hg") == "HG-0501", u"%s: hg が想定と異なります (%r)" % (ID_1_1, x.get("hg"))

    steps = x.get("steps") or []
    assert len(steps) == 4, u"%s: steps が想定(4個)と異なります(%d個)" % (ID_1_1, len(steps))
    s2 = steps[2]
    if s2.get("meaning") == NEW_M2_1_1:
        return  # 冪等
    assert s2.get("answer") == u"4", u"%s steps[2]: answer が想定と異なります" % ID_1_1
    assert s2.get("meaning") == OLD_M2_1_1, (
        u"%s steps[2]: meaning が想定と異なります（別セッションが書いた可能性）" % ID_1_1
    )
    s2["meaning"] = NEW_M2_1_1
    fixed.append(u"%s(HG-0501): steps[2]の解説が『（61・72・83・94）』とsteps[3]の答え(94)を"
                 u"先出ししていたのを列挙なしの結論だけに縮小" % ID_1_1)


FIXERS = [fix_610_6, fix_3_1, fix_21_3, fix_1_1]
TARGET_IDS = {ID_610_6, ID_3_1, ID_21_3, ID_1_1}


def main():
    path = sys.argv[1] if len(sys.argv) > 1 else DEFAULT_PATH
    d = json.load(io.open(path, encoding="utf-8"))

    fixed = []
    seen = set()
    for rec in iter_daimon(d):
        x = rec["x"]
        for fn in FIXERS:
            fn(x, fixed, seen)

    missing = TARGET_IDS - seen
    assert not missing, u"対象の大問が見つかりませんでした: %s" % sorted(missing)

    print(u"修正件数: %d" % len(fixed))
    for f in fixed:
        print(u" - " + f)

    out = json.dumps(d, ensure_ascii=False, indent=1).encode("utf-8")
    io.open(path, "wb").write(out)


if __name__ == "__main__":
    main()
